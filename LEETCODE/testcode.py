dicts = ["cat", "bat", "rat",'thap','nguyen']

sentence = "the cattle was rattled by the battery"
print(sentence.find('the'))
# newdict = {}
# for each in dicts:
#     t = len(each)
#     if t in newdict:
#         newdict[t].append(each)
#     if t not in newdict:
#         newdict[t] = [each]
# min_dict = min(list(newdict.keys()))
# sens = sentence.split(' ')
# print(newdict)
# for i in range(0, len(sens)):
#     t = len(sens[i])
#     if t < min_dict:
#         continue
#     for k in sorted(list(newdict.keys())):
#         for item in newdict[k]:
#             if sens[i].find(item) == -1:
#                 continue
#             else:
#                 sens[i] = item
#                 break
#         continue
# print(sens)


# for each in sens:
#     for item in newdict[3]:
#         if each.find(item) != 1:
#             print(item)
#             break

    

